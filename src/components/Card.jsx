function Card({cardName, setActiveCard}) {
  //onclick scryfall search, update picture
  //setActiveCard("scryfall.imagelink")
  return (
    <div style={{"border":"1px solid grey"}}>
      <p>{playerData.playerName}</p>
    </div>
  )
}

export default Card
